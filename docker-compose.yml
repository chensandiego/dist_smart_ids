version: '3.8'

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: smart_ids
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"

  elasticsearch:
    image: elasticsearch:7.17.0
    environment:
      - discovery.type=single-node
    ports:
      - "9200:9200"

  aggregator:
    build:
      context: ./aggregator
      dockerfile: Dockerfile
    image: dist_smart_ids_aggregator:latest
    depends_on:
      - postgres
      - rabbitmq
      - elasticsearch
    environment:
      - DB_HOST=postgres
      - RABBITMQ_HOST=rabbitmq
      - ELASTICSEARCH_HOST=elasticsearch

  suricata:
    build:
      context: ./suricata
      dockerfile: Dockerfile
    network_mode: host
    volumes:
      - ./suricata/rules:/etc/suricata/rules
      - suricata_logs:/var/log/suricata

  rule_updater:
    build:
      context: ./rule_updater
      dockerfile: Dockerfile
    volumes:
      - ./suricata/rules:/app/rules

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      - aggregator
    environment:
      - AGGREGATOR_BASE_URL=http://aggregator:5000

  image-scan:
    image: aquasec/trivy:latest
    command: image --severity HIGH,CRITICAL dist_smart_ids_aggregator:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  falco:
    image: falcosecurity/falco:latest
    privileged: true
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
      - /dev:/host/dev
      - /proc:/host/proc:ro
      - ./falco/falco.yaml:/etc/falco/falco.yaml
      - ./falco/rules.d:/etc/falco/rules.d
    command: falco -c /etc/falco/falco.yaml -r /etc/falco/falco_rules.yaml -r /etc/falco/rules.d/custom_rules.yaml

volumes:
  suricata_logs: